[project]
name = "radiation"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "webtypy==0.1.7",
    "starlette",
]

[dependency-groups]
dev = [
    "ruff",
    "workers-py",
    "workers-runtime-sdk",
]

[tool.ruff]
src = ["src"]
line-length = 100
