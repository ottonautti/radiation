[project]
name = "radiation"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "webtypy==0.1.7",
    "fastapi==0.129.2",
    "httpx==0.28.1",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
